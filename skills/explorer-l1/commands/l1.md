---
description: L1 cross-service stitch from accepted Descobrir L0 baselines
---

<!-- l1-install-owned:v1 -->

Run the **l1** skill.

User arguments: $ARGUMENTS

You own the runtime. Prefer the installed CLI:

`node ~/.agents/skills/l1/cli.mjs …`

Default Acme proof when args are thin:

- namespace `acme`
- system-namespace `acme-system`
- pair `acme-tax` + `tax-provider-controller` if both accepted and paths known

Do not reindex with Graphify. Do not merge L0 namespaces. Edges are
`contract-matched` only. After stitch, suggest `/graph-system` for impact queries.
